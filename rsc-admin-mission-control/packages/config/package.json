{
  "name": "@rsc/config",
  "version": "1.0.0",
  "private": true,
  "description": "Shared configuration for RSC Admin Mission Control",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "eslint": "^8.55.0",
    "typescript": "^5.3.0"
  }
}
